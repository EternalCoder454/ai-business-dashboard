# Optional. If set, the server uses this key when the browser has not stored one
# in Settings. The key entered in Settings is stored in IndexedDB on this machine
# and forwarded per-request; it is never written to disk by the server.
ANTHROPIC_API_KEY=sk-ant-...
